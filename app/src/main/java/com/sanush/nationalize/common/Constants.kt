package com.sanush.nationalize.common

/**
 * Contain all the constants.
 * Refactor this with config .properties if the app integrate with multiple variants. (ex: dev, qa, prod)
 */
object Constants {
    const val BASE_URL = "https://api.nationalize.io/"
}